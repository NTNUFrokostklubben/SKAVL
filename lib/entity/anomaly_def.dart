/// Enum to declare if an image has been classified as an anomaly by the user
/// or not. All images are Undefined by default from the analysis.
enum AnomalyDef {
  undefined,
  anomaly,
  noAnomaly,
}